# Secrets — never commit
.env

# Call artifacts — recordings + transcripts in calls/ are committed deliverables.
# Only scratch WAVs (temp channel splits) are ignored.
*.wav

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenvs
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/
